import { CountDown } from './count-down';
export class Table {
  id: number;
  size: number;
  createdDate: string;
  visible: boolean;
  tableNumber: number;
  tableStatus: number;
  countDown: CountDown;
}
